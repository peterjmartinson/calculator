var assert = require('assert');
var quote  = require('../public/js/random_quote.js');

describe('Random Quote', function() {
  describe('getQuote()', function() {
    it('should retrieve a quote', function(done) {
      getQuote(done);
    });
  });
});
